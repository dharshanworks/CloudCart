apiVersion: v1
kind: Service

metadata:
  name: cloudcart-backend-service
  namespace: cloudcart

spec:
  selector:
    app: cloudcart-backend

  ports:
    - port: 5000
      targetPort: 5000

  type: ClusterIP